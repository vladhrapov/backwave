import React, { Component } from "react";

export default class Logger extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div>
        LOGGER components settings
        1) if the logger is enabled
        2) Report type
        3) Report chart
        4) x and y axis for compare
        5) 
      </div>
    );
  }
} 